{
  "mcpServers": {
    "grafana-cloud": {
      "command": "npx",
      "args": ["-y", "@grafana/mcp-grafana@latest"],
      "env": {
        "GRAFANA_URL": "${GRAFANA_URL}",
        "GRAFANA_API_KEY": "${GRAFANA_API_KEY}"
      },
      "_comment": "Official Grafana Cloud MCP server. Used by DirectorAgent at runtime via tools=['mcp:grafana-cloud'] to gate Veo video generation based on system health."
    },
    "reality-mcp": {
      "command": "python",
      "args": ["mcp_servers/reality_mcp.py"],
      "env": {},
      "_comment": "Custom world-state MCP server. Injects weather and news headlines into character context."
    }
  }
}
